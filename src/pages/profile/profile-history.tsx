import { InquiryHistoryGuestPage } from '@/components/inquiry-history'

/**
 * Profile Transaction History - authenticated Guest view of inquiry/transaction history.
 * Renders InquiryHistoryGuestPage with filters, pagination, and detail modal.
 */
export function ProfileHistory() {
  return <InquiryHistoryGuestPage />
}
